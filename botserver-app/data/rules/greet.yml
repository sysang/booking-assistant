version: "2.0"

rules:
- rule: Say `hello` whenever the user sends a message with intent `greet`
  condition:
    - slot_was_set:
      - botmind_state: attentive
  steps:
  - intent: greet
  - action: utter_greet
